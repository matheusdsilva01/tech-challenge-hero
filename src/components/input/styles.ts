import { tv } from "tailwind-variants"

export const baseInput = tv({
  base: "bg-zinc-200 p-2 rounded-md border border-zinc-300 focus:outline-none focus:ring-2 focus:ring-zinc-400 focus:border-transparent",
})
